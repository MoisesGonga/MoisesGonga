class DirectionDetail{

  final String distanceText;
  final int distanceValeu;
  final String durationText;
  final int durationValeu;
  final String encodedPoints;

  DirectionDetail(this.distanceText, this.distanceValeu, this.durationText,
      this.durationValeu, this.encodedPoints);

}